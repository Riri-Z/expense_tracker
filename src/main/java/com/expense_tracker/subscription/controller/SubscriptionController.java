package com.expense_tracker.subscription.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/auth")
public class SubscriptionController {

	/*
	 * private static final Logger log =
	 * LoggerFactory.getLogger(SubscriptionController.class);
	 *
	 * private final SubscriptionService service;
	 *
	 * private final JwtService jwtService;
	 *
	 * private final AuthenticationManager authenticationManager;
	 *
	 * public SubscriptionController(@AuthenticationPrincipal
	 * AuthenticationManager,SubscriptionService service, JwtService jwtService,
	 * AuthenticationManager authenticationManager) { this.service = service;
	 * this.jwtService = jwtService; this.authenticationManager = authenticationManager; }
	 */

}
